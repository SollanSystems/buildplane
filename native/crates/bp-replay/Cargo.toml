[package]
name = "bp-replay"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bp-ledger.workspace = true
chrono.workspace = true
rusqlite.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
uuid.workspace = true

[dev-dependencies]
# Replay fixtures reconstruct historical tapes containing kinds the public
# append paths now refuse, so the integration tests need bp-ledger's gated
# insert helper. Mirrors the bp-authority-broker dev-dependency idiom.
bp-ledger = { workspace = true, features = ["test-support"] }
ed25519-dalek.workspace = true
tempfile = "3"
