---
"buildplane": minor
---

planforge: add `buildDefaultCapabilityBundleForPlan` — the run-wide capability envelope (deterministic sorted union of every task's `fsWrite` globs + `run_command` allowlist) derived from an admitted plan (M3-S7).
