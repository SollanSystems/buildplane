---
"@buildplane/capability-broker": minor
---

Add `@buildplane/capability-broker` package: CapabilityBundleV0 schema, parse/validate (fail closed), and `bundleDigest` via PlanForge canonical digest (M3-S1).