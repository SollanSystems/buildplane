{
	"name": "buildplane",
	"version": "0.1.0",
	"description": "Host-aware AI execution control plane — typed work units, evidence capture, policy gates, scoped memory, self-learning flywheel",
	"private": true,
	"engines": {
		"node": "24.13.1"
	},
	"scripts": {
		"dev": "cd main && pnpm install && pnpm test"
	}
}
