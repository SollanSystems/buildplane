{
  "name": "buildplane-workspace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo 'TODO: implement workspace build'",
    "test": "echo 'TODO: add workspace tests'",
    "lint": "echo 'TODO: add workspace lint'"
  }
}
