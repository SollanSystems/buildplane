# Buildplane slice receipt — M6-F1 fail-closed PlanForge resume/recover

## Slice identity

- Slice id: M6-F1
- Milestone: M6 (end-to-end demo + v0.5.0 cut)
- Goal: close the crash-recovery fail-open — `planforge resume`/`recover` must run the
  remaining suffix under the M4 acceptance gate AND only count a recorded-prefix
  activity toward a `completed` receipt when the tape carries a matching signed
  `acceptance_recorded` verdict; otherwise fail-close with a machine-readable reason
  and reconcile the orphaned `running` storage row.
- Non-goals: no new ledger event kind, no Rust change, no fixture regeneration, no new
  `packages/*` import into `apps/cli`; the sync `runPacket` path is untouched;
  `emitPlanForgeTerminalReceipt` dedup semantics untouched.
- Operator approval scope: implement AC1–AC7 exactly (locked design D1–D6); commit
  locally only (no push / no PR).
- Steward / agent: TDD implementer subagent
- Started at: 2026-07-03
- Completed at: 2026-07-03

## Source of truth

- Base branch: `main`
- Base SHA: `236dddf`
- Source docs / specs read: repo `CLAUDE.md` (M2 crash-recovery contract, L0 trust
  surface, gotchas); `packages/planforge/src/acceptance-contract.ts`;
  `apps/cli/src/ledger-acceptance.ts`; `apps/cli/src/ledger-result-ready.ts`; existing
  ledger-integration crash/resume/recover harnesses.
- Related issue / task / board item: M6-F1 (fail-closed resume) — from the 2026-07-03
  watched M6 demo finding "crash recovery is receipt-grade, not pipeline-grade".
- Prior prerequisite PRs verified on `origin/main`: M6-S7 (`result_ready`/`run_completed`
  emit, `fb96406`) present; M6-S11 crash-injection guard present.

## Workspace

- Worktree path: `/mnt/c/Dev/projects/buildplane/.claude/worktrees/wf_b642ff31-439-1`
- Branch: `fix/m6-f1-resume-fail-closed`
- Local HEAD SHA: see structured output (committed locally)
- Remote branch SHA: n/a (not pushed — local only per operator scope)
- Git identity verified: yes (Sollan Systems)
- `core.filemode=false` verified: n/a (no filemode-sensitive changes)
- Clean status before work: yes (clean worktree at `236dddf`)
- Clean status after commit: yes

## Scope

- Files changed:
  - `apps/cli/src/run-cli.ts` — `resumePlanForgePlanFromInput` fail-closed rewrite;
    extracted shared `deriveDispatchAcceptanceProfiles` helper (byte-equivalent to the
    dispatch block, D1); `readPlanForgeReplayState` now also reads `acceptance_recorded`;
    `reconcilePlanForgeRunningRuns` helper (D4); `resume`/`recover` accept
    `--no-enforce-acceptance` (D3); CLI help text.
  - `packages/kernel/src/ports.ts` — new `BuildplaneStoragePort.reconcilePlanForgeDispatchRuns`.
  - `packages/storage/src/store.ts` — implement `reconcilePlanForgeDispatchRuns`.
  - `docs/operations/2026-07-02-m6-demo-runbook.md` — Property-1 narration → fail-closed.
  - tests: `planforge-resume`, `planforge-recover`, `demo-crash-resume`, `crash-replay`,
    `planforge-m2-vertical-slice` (semantics updated + new fail-closed / opt-out /
    contract-violation / idempotent-reconcile coverage).
  - `apps/cli/test/fixtures/planforge/goal-input-shared-digest.md` — new two-task fixture
    whose tasks share an acceptance contract digest (repair regression: consume-once).
- Diff stat: 10 files (~+700 / −95 including `.changeset`).
- Added dependencies: none.
- Data migrations / durable schema changes: none (new store method reuses the `runs`
  table; no schema change).
- Tool / workflow permission changes: none.
- Deployment / publish / release side effects: none (changeset patch only).
- Secret-shaped added lines scan: none (no keys/tokens; test key material is the
  pre-existing `Buffer.alloc(32, 7)` kernel seed).

## Verification

- Focused tests (each run with `env BUILDPLANE_NATIVE_BIN=<main debug binary> pnpm -C <wt> exec vitest run <path>`):
  - `test/ledger-integration/planforge-resume.test.ts` → 6 passed (incl. new
    fail-closed, `--no-enforce-acceptance` opt-out, AC1 contract-violation, and the
    repair regression: two same-digest recorded tasks with ONE acceptance verdict →
    the second fail-closes `acceptance-not-evaluated`, receipt `failed`).
  - `test/ledger-integration/planforge-recover.test.ts` → 3 passed (incl. D4 reconcile +
    AC4 second-pass `no_orphans`).
  - `test/ledger-integration/demo-crash-resume.test.ts` → 1 passed (fail-closed outcome,
    exactly-one `plan_receipt`).
  - `test/ledger-integration/crash-replay.test.ts` → 3 passed (all boundaries).
  - `test/workflow/planforge-m2-vertical-slice.test.ts` → 1 passed.
- RED-first: ran the unmodified `planforge-resume.test.ts` against the new code and
  confirmed the recorded-without-acceptance case fail-closes (exit 1) BEFORE updating
  the test to seed acceptance evidence.
- Package / area tests: `packages/storage/test` + published-bootstrap closure — see
  structured output test-evidence for the recorded result.
- Typecheck: `pnpm -C <wt> exec tsc --build --pretty false` → clean (no output).
- Lint / format: `pnpm -C <wt> exec biome check --write <changed files>` → format fixed;
  0 errors. One PRE-EXISTING `noUnusedImports` warning (`spawn` in `run-cli.ts`, present
  on `origin/main`, only referenced in comments) left untouched (out of scope).
- Build: deferred to CI `verify` (canonical; local `biome check .` OOMs per gotchas).
- Full-suite check, if run: not run locally (biome whole-repo OOM); CI `verify` is the
  canonical gate.
- Isolated reruns for flaky / timeout failures: ledger-integration files run ONE AT A
  TIME to avoid the known `process.chdir` parallel flake.
- Direct CLI smoke / manual probe: exercised end-to-end through the real signed ledger
  subprocess in every ledger-integration test (real `bp init`/`admit`/`resume`/`recover`).
- Forbidden literal / ambient-authority grep: no new secrets; enforcement decision is
  never sourced from the unsigned dispatch manifest (commented in code, D3).

### Cross-language / digest (L0 ledger, signing, replay, digest slices)

- `u64` → TS `string` boundary verified: n/a — no new signed wire field. The correlation
  reads the EXISTING `AcceptanceRecordedV1` payload (all-string fields already) and
  correlates by its `contract_digest` string.
- Canonical digest byte-stability: n/a — no digest emitted here; the recorded-prefix
  correlation reuses `acceptanceContractDigest(deriveAcceptanceContract(...))`, the same
  deterministic digest dispatch signs, so the re-derived digest is byte-stable by reuse.
- Repair (consume-once cardinality): the recorded-prefix evidence gate now consumes passed
  verdicts as a MULTISET (`Map<contractDigest, count>`), decrementing one per recorded
  task, instead of `Set.has` membership. `acceptanceContractDigest` intentionally excludes
  the task id (it hashes only `{diff_scope.allowed_globs, checks}`, derived from
  `allowedSideEffects` + `verificationCommands`), so sibling tasks with identical shapes
  share a digest D; set-membership let ONE `acceptance_recorded(D, passed)` clear EVERY
  same-digest recorded task, re-opening the fail-open for tasks whose acceptance never ran.
  Cardinality requires N distinct passed verdicts for N same-digest recorded-passed tasks.
  No L0 change — a pure `apps/cli` consumer fix over the already-signed events.
- Ledger fixture freshness: n/a — no Rust payload/typeshare change; fixtures untouched.
- Whole-workspace `cargo test`: n/a — no Rust change / no new event kind.

## Review gate

### Ceremony tier

- Tier: `L1` (kernel/dispatch/recovery logic; touches the crash-recovery trust path and
  a storage-port method, but adds NO event kind / signing / replay / digest surface — so
  not L0). 2-role: implementer TDD self-verify + independent reviewer.
- Roles satisfied: implementer TDD self-verify (this receipt). Independent reviewer:
  pending (parent orchestrator to dispatch).
- Tier justification: no `bp-ledger` kind/canonicalize/replay/wire change; the tape is
  read (new `acceptance_recorded` correlation) but not extended. The signed events emitted
  on the suffix path are the already-shipped `acceptance_recorded` / `result_ready` /
  `plan_receipt` kinds, wired identically to dispatch.

- Review task id / reviewer: pending
- Reviewed commit SHA: pending
- Current PR head SHA: n/a (local only)
- Verdict: pending
- Significant issues found: n/a
- Review notes / link: n/a

## PR gate

- Not applicable this slice — local commit only, no push / no PR (operator scope).

## Post-merge verification

- Not applicable this slice.

## Exceptions / caveats

- Known unrelated warnings: pre-existing `noUnusedImports` (`spawn`) in `run-cli.ts`.
- Local load-sensitive failures and isolated rerun evidence: ledger-integration run
  serially to dodge the documented `process.chdir` parallel flake.
- The receipt binds its `result` only by `result_digest` (never stored inline), so the
  `acceptance-not-evaluated` reason is committed to the tape via the digest and surfaced
  verbatim in the CLI JSON output — the fail-closed test asserts the failed outcome +
  a non-empty `result_digest`, and the JSON reason directly.
- `bp web` and other surfaces untouched.

## Next gate

- Next allowed action: independent L1 reviewer pass on the committed SHA, then operator
  decides push / PR.
- Actions explicitly not authorized: push, PR, merge, publish (not in scope).
- Fresh context required before continuing: no.
